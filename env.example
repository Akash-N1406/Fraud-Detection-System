# Copy to .env and fill in your actual PostgreSQL credentials.
# .env is gitignored — never commit real credentials.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=frauddb
DB_USER=postgres
DB_PASSWORD=your_password_here
